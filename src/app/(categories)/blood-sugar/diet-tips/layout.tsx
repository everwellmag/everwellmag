import { ReactNode } from 'react';

export const metadata = {
    title: 'Diet Tips for Blood Sugar - Everwell Magazine',
    description: 'Explore expert diet plans and tips to manage and stabilize your blood sugar levels with healthy, balanced meals.',
};

export default function DietTipsLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}