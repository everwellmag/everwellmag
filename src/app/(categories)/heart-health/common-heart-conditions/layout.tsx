import { ReactNode } from 'react';

export const metadata = {
    title: 'Common Heart Conditions - Everwell Magazine',
    description: 'Learn about common heart conditions, their symptoms, causes, and treatments to help you maintain cardiovascular health and well-being.',
};

export default function CommonHeartConditionsLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
