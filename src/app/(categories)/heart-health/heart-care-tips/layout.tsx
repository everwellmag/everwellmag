import { ReactNode } from 'react';

export const metadata = {
    title: 'Heart Care Tips - Everwell Magazine',
    description: 'Discover practical heart care tips to improve cardiovascular health, manage risk factors, and support long-term heart wellness.',
};

export default function HeartCareTipsLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
