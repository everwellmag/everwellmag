import { ReactNode } from 'react';

export const metadata = {
    title: "Common Men's Health Issues - Everwell Magazine",
    description: 'Learn about common men’s health issues, their symptoms, causes, and effective strategies to improve overall well-being and quality of life.',
};

export default function CommonMensHealthIssuesLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
