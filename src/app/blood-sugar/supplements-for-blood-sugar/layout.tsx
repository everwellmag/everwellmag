import { ReactNode } from 'react';

export const metadata = {
    title: 'Supplements for Blood Sugar - Everwell Magazine',
    description: 'Explore effective supplements that may support healthy blood sugar levels, with insights on ingredients, benefits, and expert recommendations.',
};

export default function SupplementsForBloodSugarLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
