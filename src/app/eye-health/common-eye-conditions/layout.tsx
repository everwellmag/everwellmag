import { ReactNode } from 'react';

export const metadata = {
    title: 'Common Eye Conditions - Everwell Magazine',
    description: 'Understand common eye conditions, their causes, symptoms, and treatments to help you maintain better vision and overall eye health.',
};

export default function CommonEyeConditionsLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
