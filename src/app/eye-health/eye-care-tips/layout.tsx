import { ReactNode } from 'react';

export const metadata = {
    title: 'Eye Care Tips - Everwell Magazine',
    description: 'Discover practical eye care tips to protect your vision, reduce eye strain, and maintain long-term eye health with expert guidance.',
};

export default function EyeCareTipsLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
