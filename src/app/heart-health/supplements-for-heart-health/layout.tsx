import { ReactNode } from 'react';

export const metadata = {
    title: 'Supplements for Heart Health - Everwell Magazine',
    description: 'Explore supplements that may support heart health, with insights on essential nutrients, benefits, and expert recommendations for cardiovascular wellness.',
};

export default function SupplementsForHeartHealthLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
