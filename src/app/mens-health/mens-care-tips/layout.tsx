import { ReactNode } from 'react';

export const metadata = {
    title: "Men's Care Tips - Everwell Magazine",
    description: 'Discover practical men’s care tips to support physical health, mental well-being, and overall lifestyle improvement.',
};

export default function MensCareTipsLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
