import { ReactNode } from 'react';

export const metadata = {
    title: "Common Women's Health Issues - Everwell Magazine",
    description: 'Learn about common women’s health issues, their symptoms, causes, and effective strategies to support long-term wellness and quality of life.',
};

export default function CommonWomensHealthIssuesLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
