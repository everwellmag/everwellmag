import { ReactNode } from 'react';

export const metadata = {
    title: "Women's Care Tips - Everwell Magazine",
    description: 'Explore practical women’s care tips to support physical health, emotional well-being, and a balanced lifestyle.',
};

export default function WomensCareTipsLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
